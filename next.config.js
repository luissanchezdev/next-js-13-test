/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'api.dicebear.com',
            'i.pravatar.cc'
        ]
    }
};

module.exports = nextConfig;
